# Image Occlusion

An isolated prompt and authoring extension for drawing accessible image masks. The authoring frame has no network access and returns only media and mask drafts selected by the user.
